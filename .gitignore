*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.claude/settings.local.json

# Release signing — never commit the keystore or its passwords.
keystore.properties
*.jks
*.keystore

# Firebase config — not a traditional secret (scoped by package name + SHA-1), but kept local
# since this repo is public; copy from the Firebase Console after registering the app.
app/google-services.json
